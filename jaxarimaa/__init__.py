"""jaxarimaa: a vectorized, JAX-native Arimaa engine + AlphaZero training stack.

This package is the migration target off muzero-general (see docs/JAX_REBASE_SCOPE.md).
The legacy Python engine at ``games/arimaa.py`` is retained as the *reference oracle*
for differential-testing the vectorized JAX engine implemented here.
"""
